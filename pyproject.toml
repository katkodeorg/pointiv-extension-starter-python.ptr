[project]
name = "hello-world-extension"
version = "1.0.2"
description = "Minimal Pointiv community extension (WASM/Python)"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
