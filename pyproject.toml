[project]
name = "hello-world-extension"
version = "1.2.0"
description = "Pointiv community extension (WASM/Python)"
requires-python = ">=3.10"
dependencies = ["pointiv-extension-sdk>=0.4.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
